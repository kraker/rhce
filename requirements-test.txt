# Testing dependencies for RHCE study repository
# Focuses on Ansible playbook validation and syntax checking

# Ansible testing framework (without molecule integration testing)
pytest>=7.0.0
pytest-ansible>=4.0.0
pytest-xdist>=3.0.0  # Parallel test execution

# Ansible validation and testing utilities
ansible-runner>=2.3.0  # Programmatic Ansible execution
ansible-test>=1.0.0  # Ansible's built-in testing tools

# YAML and configuration testing
pyyaml>=6.0
jsonschema>=4.0.0  # Validate JSON/YAML schemas

# Testing utilities
testinfra>=10.0.0  # Infrastructure testing
paramiko>=3.0.0  # SSH testing for connectivity
requests>=2.31.0  # HTTP testing for web services

# Coverage and reporting
pytest-cov>=5.0.0
pytest-html>=4.0.0  # HTML test reports

# Ansible inventory testing
ansible-inventory-grapher>=2.5.0  # Visualize inventory relationships